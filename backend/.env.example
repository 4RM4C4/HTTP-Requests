# Server
PORT=3001

# JWT secret — use a long random string in production
SECRET=change_this_to_a_long_random_secret

# Main database
DATABASE_URL=mariadb://user:password@host:3306/database

# Test database (used by npm run test:integration)
DATABASE_TEST_URL=mariadb://user:password@host:3306/database_test
